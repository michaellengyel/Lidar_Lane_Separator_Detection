#include "gtest/gtest.h"

TEST(LSD_Tests, Prototype_Test_1) {
  // IF

  // WHEN

  // THEN
  EXPECT_EQ(1, 1);
}

TEST(LSD_Tests, Prototype_Test_2) {
  // IF

  // WHEN

  // THEN
  EXPECT_EQ(false, false);
}

TEST(LSD_Tests, Prototype_Test_3) {
  // IF

  // WHEN

  // THEN
  EXPECT_EQ(1.0f, 1.0f);
}
